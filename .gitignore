# Rust / Cargo
/target/
**/*.rs.bk
*.pdb

# WebAssembly / Trunk
/dist/
/pkg/
.trunk/
wasm-pack.log
# Trunk build artefacts also land inside each crate's own
# `dist/`/`pkg/` (e.g. `crates/flow-lenia-web/dist/`); the leading
# `/` form above only matches the workspace root.
**/dist/
**/pkg/

# Node (任意で frontend tooling を入れる場合)
/node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE / エディタ
.vscode/
.idea/
*.swp
*.swo
*~
.zed/

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# 環境変数 / シークレット
.env
.env.local
.env.*.local

# ビルド成果物 / プロファイリング
*.profraw
*.profdata
perf.data
perf.data.old
flamegraph.svg

# ローカル設定
.claude/settings.local.json

# JAX reference repo (LICENSE 不明のためローカルのみ。再取得手順は README 参照)
/references/FlowLenia-jax/

# Python venv for fixture-generation scripts (see scripts/generate_jax_fixtures.py)
/.venv-fixtures/

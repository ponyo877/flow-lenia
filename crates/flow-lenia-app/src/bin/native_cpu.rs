#![deny(warnings)]
//! Native CPU binary for Flow-Lenia.
//!
//! Runs the CPU reference implementation (see `flow-lenia-core`) with terminal
//! visualization. Fully implemented in M1.14 per `DESIGN.md` (Rev. 4) §8.

fn main() {
    println!("flow-lenia native_cpu — implemented in M1.14");
}

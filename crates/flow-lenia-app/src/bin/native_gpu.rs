#![deny(warnings)]
//! Native GPU binary for Flow-Lenia.
//!
//! Opens a `winit` window and runs the wgpu compute pipeline (see
//! `flow-lenia-gpu`) with real-time visualization. Fully implemented in M2.10
//! per `DESIGN.md` (Rev. 4) §8.

fn main() {
    println!("flow-lenia native_gpu — implemented in M2.10");
}
